# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Parser API (Netlify function)
# For production (Vercel): use full URL like https://your-site.netlify.app/.netlify/functions/parse
# For local development: use /.netlify/functions/parse (when running netlify dev)
NEXT_PUBLIC_PARSE_FUNCTION_URL=/.netlify/functions/parse

# AI Summary Function (Netlify function) - Optional
# If not set, will be derived automatically from NEXT_PUBLIC_PARSE_FUNCTION_URL
# NEXT_PUBLIC_SUMMARY_FUNCTION_URL=https://your-site.netlify.app/.netlify/functions/generate-summary

# Cron Job Security (generate a random string)
# Example: openssl rand -base64 32
CRON_SECRET=your-random-secret-token-here
